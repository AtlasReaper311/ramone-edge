{
  "name": "ramone-edge",
  "version": "1.0.0",
  "private": true,
  "description": "Public Worker fronting the Ramone local-AI tunnel. Turnstile, rate-limited, SSE pass-through.",
  "type": "module",
  "scripts": {
    "build:icons": "node scripts/embed-browser-icons.mjs",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/index.js && node --check src/ask.js && node --check src/cors.js && node --check src/frontend.js && node --check src/frontend-core.js && node --check src/browser-icons.js && node --check src/browser-icons.generated.js && node --check src/notify.js && node --check src/ratelimit.js && node --check src/status.js && node --check scripts/embed-browser-icons.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  },
  "engines": {
    "node": ">=22"
  }
}
