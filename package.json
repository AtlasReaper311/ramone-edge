{
  "name": "ramone-edge",
  "version": "1.0.0",
  "private": true,
  "description": "Public Worker fronting the Ramone local-AI tunnel. Turnstile, rate-limited, SSE pass-through.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/index.js && node --check src/ask.js && node --check src/cors.js && node --check src/frontend.js && node --check src/notify.js && node --check src/ratelimit.js && node --check src/status.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "eslint": "^9.16.0",
    "globals": "^17.7.0",
    "vitest": "^4.1.10",
    "wrangler": "^3.114.17"
  },
  "engines": {
    "node": ">=22"
  }
}
