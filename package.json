{
  "name": "ramone-edge",
  "version": "1.0.0",
  "private": true,
  "description": "Public Worker fronting the Ramone local-AI tunnel. Turnstile, rate-limited, SSE pass-through.",
  "type": "module",
  "scripts": {
    "build:icons": "node scripts/embed-browser-icons.mjs",
    "build:interface": "node scripts/embed-interface-bundle.mjs",
    "verify:interface": "node scripts/verify-interface-bundle.mjs",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/index.js && node --check scripts/embed-browser-icons.mjs && node --check scripts/embed-interface-bundle.mjs && node --check scripts/verify-interface-bundle.mjs"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.131.0"
  },
  "overrides": {
    "brace-expansion": "5.0.9"
  },
  "engines": {
    "node": ">=22"
  }
}
